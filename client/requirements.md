## Packages
framer-motion | Smooth, minimalist animations and transitions
lucide-react | Standard icons (already installed, but explicitly required for minimalist icons)

## Notes
The app uses purely local file input and object URLs for audio playback to maintain maximum privacy and speed.
History is persisted to the backend to fulfill requirements.
